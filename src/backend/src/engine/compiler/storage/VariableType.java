package engine.compiler.storage;

/**
 * This enum class includes the type of variables to be stored. This serves to replace the String architecture of identifying variables.
 *
 * @author Haotian Wang
 */
public enum VariableType {
    STRING,
    DOUBLE,
    INTEGER,
    EXPRESSION
}
